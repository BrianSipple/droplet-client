import windowProxy from 'droplet/utils/window-proxy';
import { module, skip } from 'qunit';

module('Unit | Utility | window proxy');

// Replace this with your real tests.
skip('it works', function(assert) {
  const result = windowProxy();
  assert.ok(result);
});

import makeDate from 'droplet/utils/make-date';
import { module, test } from 'qunit';

module('Unit | Utility | make date');

// Replace this with your real tests.
test('it works', function(assert) {
  const result = makeDate();
  assert.ok(result);
});

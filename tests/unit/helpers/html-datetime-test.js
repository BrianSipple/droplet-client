import { htmlDatetime } from 'droplet/helpers/html-datetime';
import { module, test } from 'qunit';

module('Unit | Helper | html datetime');

// Replace this with your real tests.
test('it works', function(assert) {
  const result = htmlDatetime([42]);
  assert.ok(result);
});
